<thead><tr><td><%= milestone.title %></td></tr></thead>
<tbody>
  <% milestone.entries.each {|entry| %>
  <%= render :partial => 'lesson_plan/entry', locals: { :entry => entry } %>
  <% } %>
</tbody>
