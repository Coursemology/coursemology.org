<div class="page-header">
  <h1><%= @mission.title %></h1>
</div>


<% can_start, reason = @mission.can_start? curr_user_course %>

<% unless can_start %>
    <p class="mission-not-opened"> <%= reason %></p>
<% end %>