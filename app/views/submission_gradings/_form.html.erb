<table>
  <tr><td>Student: </td><td><%= @submission.student.name %></td></tr>
  <tr><td>Submited at: </td><td><%= @submission.created_at %></td></tr>
</table>
<hr/>

<p class="well"><%= @assignment.description %></p>

<%= form_for [@course, @assignment, @submission, @submission_grading],
             html: { class: "form-horizontal" } do |f| %>
  <input type="hidden" name="submission_id" value="<% @submission.id %>" />

  <% @qadata.each do |qid, qa| %>
    <hr/>
    <h4>Question:</h4>
    <p><%= qa[:q].description %></p>
    <% if qa[:a] %>
      <h4>Answer:</h4>
      <p><%= qa[:a].text %></p>
      <input type="hidden" name="ags[][student_answer_id]" value="<%= qa[:a].id %>">
      <div class="control-group">
        <label class="control-label">Grade</label>
        <div class="controls">
          <input type="text" name="ags[][grade]" value="<%= qa[:g].grade if qa[:g] %>" />
          / <%= qa[:q].max_grade %>
        </div>
      </div>
      <div class="control-group">
        <label class="control-label">Comment</label>
        <div class="controls">
          <textarea name="ags[][comment]" class="span9" rows="5"><%= qa[:g].comment if qa[:g] %></textarea>
        </div>
      </div>
    <% else %>
      <h4>No answer</h4>
    <% end %>
  <% end %>
  <div class="form-actions">
    <%= f.submit "Save Grading", :class => 'btn btn-primary' %>
    <button type="button" class="btn">Cancel</button>
  </div>
<% end %>
