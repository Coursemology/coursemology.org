<%
=begin
Require: @course
=end
%>

<header class="course-banner">
  <div class="row-fluid">
    <table>
      <tr>
        <td class="span2">
          <img class="pull-left" src="<%= @course.logo_url %>" />
        </td>
        <td class="span10">
          <h1 class="banner-text"><%= @course.title %></h1>
        </td>
      </tr>
    </table>
</div>
</header>
